
## requirements
install.packages(c("forecast", "car", "broom", "opera", "TSA", "tseries", "ggplot2",
    "reshape2", "zoo", "mgcv", "fracdiff", "moments", "settings"))

## suggests (for running tests)
install.packages(c("testthat", "covr"))

## EXAMPLE, CUSTOMIZE TO YOUR LOCAL ENVIRONMENT
install.packages("~/forecastR_0.1.0.tar.gz", repos = NULL)
